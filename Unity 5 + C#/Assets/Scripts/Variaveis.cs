using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Variaveis : MonoBehaviour
{
    //variéveis (int, float, string, bool)
    //tipo de acesso (opcional) + tipo de variável + nome da variável(obrigatório) + = valor(opcional)

    public int meuInteiro;
    public float meuPontoFlutuante;
    public string meuTexto;
    public bool meuBooleano;

}
